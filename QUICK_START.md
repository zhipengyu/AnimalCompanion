# 产品展示区域 - 快速使用指南

## ✅ 已完成的工作

### 1. 创建了产品展示组件
**文件位置**: `src/components/ProductsSection.vue`

这是一个完整的产品展示组件，包含：
- 9个产品卡片（使用本地图片）
- 响应式网格布局
- 精美的悬停动画效果
- 产品分类、标题、描述、价格信息
- 购物车按钮和查看详情功能

### 2. 集成到首页
**修改文件**: `src/views/Home.vue`

已将ProductsSection组件添加到首页，位于"产品分类"和"关于我们"之间。

### 3. 图片映射
所有产品都使用了 `src/assets/img/` 目录下的本地图片：

| 产品类别 | 产品名称 | 使用图片 |
|---------|---------|---------|
| 猫咪专区 | 精选猫粮套装 | cat.jpeg |
| 狗狗专区 | 优质狗粮系列 | dog.png |
| 宠物用品 | 宠物生活套装 | pets.png |
| 猫咪玩具 | 互动猫咪玩具 | 生成特定猫咪图片.png |
| 宠物零食 | 营养零食礼包 | 2).png |
| 护理用品 | 宠物美容套装 | 3).png |
| 智能用品 | 智能喂食器 | 5.png |
| 户外用品 | 外出便携套装 | 6.png |
| 猫咪窝垫 | 舒适猫窝系列 | 生成特定猫咪图片 (1).png |

## 🚀 如何运行

```bash
# 1. 确保已安装依赖
npm install

# 2. 启动开发服务器
npm run serve

# 3. 在浏览器中访问
# 通常是 http://localhost:8080
```

## 🎨 设计特点

### 卡片布局
```
┌─────────────────────────┐
│                         │
│    产品图片 (280px)      │
│   (悬停时放大+遮罩)      │
│                         │
├─────────────────────────┤
│  [分类标签]              │
│  产品标题                │
│  产品描述...             │
│                         │
│  ¥价格  [加入购物车]     │
└─────────────────────────┘
```

### 响应式断点
- **手机** (< 768px): 1列
- **平板** (768px - 1024px): 2列
- **电脑** (> 1024px): 3列自适应

### 交互效果
1. **悬停卡片**: 上浮8px + 粉色阴影
2. **悬停图片**: 放大1.1倍
3. **显示遮罩**: 黑色半透明 + "查看详情"按钮
4. **按钮动效**: 颜色渐变 + 轻微缩放

## 📝 自定义产品

### 添加新产品
在 `ProductsSection.vue` 中的 `products` 数组添加：

```javascript
{
  category: '产品分类',
  title: '产品名称',
  description: '产品描述文字',
  price: '¥XX-XX',
  image: require('@/assets/img/your-image.png')
}
```

### 修改现有产品
直接编辑 `products` 数组中的对应项即可。

### 更换图片
1. 将新图片放入 `src/assets/img/`
2. 修改对应产品的 `image` 字段
3. 使用 `require('@/assets/img/新图片名.png')`

## 🎯 组件结构

```
ProductsSection.vue
├── template (模板)
│   ├── section.products
│   │   ├── 标题和副标题
│   │   ├── products-grid (产品网格)
│   │   │   └── product-card × 9 (产品卡片)
│   │   │       ├── product-image-wrapper (图片容器)
│   │   │       │   ├── img (产品图片)
│   │   │       │   └── product-overlay (遮罩层)
│   │   │       └── product-info (产品信息)
│   │   │           ├── category (分类)
│   │   │           ├── title (标题)
│   │   │           ├── description (描述)
│   │   │           └── product-footer (底部)
│   │   │               ├── price (价格)
│   │   │               └── btn-add-cart (按钮)
│   │   └── view-more (查看更多)
├── script (脚本)
│   └── data.products[] (产品数据)
└── style (样式)
    └── scoped CSS (局部样式)
```

## 🔧 常见问题

### Q: 图片不显示？
A: 检查以下几点：
1. 图片文件是否存在于 `src/assets/img/`
2. 文件名是否正确（包括大小写）
3. 使用 `require('@/assets/img/...')` 而不是直接路径

### Q: 如何修改颜色主题？
A: 在 `<style scoped>` 中查找并替换：
- `#FF6B9D` (主粉色)
- `#C06C84` (深粉色)

### Q: 如何调整卡片数量？
A: 修改 `products` 数组，添加或删除产品对象。

### Q: 如何禁用悬停效果？
A: 删除或注释掉相关的 `:hover` CSS规则。

## 📦 文件清单

```
项目根目录/
├── src/
│   ├── components/
│   │   └── ProductsSection.vue  ← 新建的产品组件
│   ├── views/
│   │   └── Home.vue             ← 已修改（引入ProductsSection）
│   └── assets/
│       └── img/                 ← 产品图片目录
│           ├── cat.jpeg
│           ├── dog.png
│           ├── pets.png
│           ├── 2).png
│           ├── 3).png
│           ├── 5.png
│           ├── 6.png
│           ├── 生成特定猫咪图片.png
│           └── 生成特定猫咪图片 (1).png
└── PRODUCTS_SECTION_README.md   ← 详细文档
```

## 🌟 效果预览

访问首页后，您将看到：

1. **顶部**: 英雄区轮播图
2. **第二区**: 产品分类（6个图标卡片）
3. **第三区**: ⭐ **精选产品展示**（9个图片卡片）← 新增
4. **第四区**: 关于我们
5. **底部**: 联系方式

## 💡 下一步建议

- [ ] 优化图片大小（当前3-5MB较大）
- [ ] 添加产品详情页
- [ ] 实现购物车功能
- [ ] 添加产品筛选和搜索
- [ ] 接入后端API获取产品数据
- [ ] 添加图片懒加载

---

**开发完成** ✅
如有问题，请查看 `PRODUCTS_SECTION_README.md` 获取更多详细信息。
